@model EnterpriseMS.ViewComponents.PersonSelectViewModel
<select id="@Model.Id" name="@Model.Name" class="@Model.CssClass" onchange="@Model.Onchange">
    @if (!string.IsNullOrEmpty(Model.EmptyText))
    {
        <option value="">@Model.EmptyText</option>
    }
    @foreach (var p in Model.Persons)
    {
        <option value="@p.Id" selected="@(Model.SelectedId == p.Id ? "selected" : null)">@p.Display</option>
    }
</select>
