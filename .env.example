# 本文件仅作模板/文档说明。.NET 不会自动读取 .env，部署时请把下列变量设为真实环境变量：
#   - 本地 VS2022 调试：在 Properties/launchSettings.json 的 environmentVariables 中设置
#   - Docker / docker compose：用 .env 文件注入容器（compose 会读取 .env 并传给容器环境）
#   - 物理机/Systemd/IIS：在操作系统的环境变量或服务的环境配置中设置
# 应用启动时会用这些环境变量覆盖 appsettings 中的 ${...} 占位符。

# 数据库（MySQL）
DB_HOST=192.168.1.100
DB_PORT=3306
DB_NAME=enterprise_db
DB_USER=root
DB_PASSWORD=你的数据库密码

# Redis
REDIS_HOST=192.168.1.100
REDIS_PORT=6379
REDIS_PASSWORD=你的Redis密码
