# EnterpriseMS 代码审计修复总结（续）2026-07-24

> 范围：按用户指示，**除 2.1（IUnitOfWork 拆分）与 3.6（CrudControllerBase）外，全部按建议修复，时区统一为 UTC**。
> 验证：`dotnet build -c Debug` → **0 错误**。

## 一、2.3 Controller 越层持久化下沉（全部完成）

审计原表：KbController / ProjectImportController / DebugController / ReportController / AccountController / ProjectController 直接注入 `AppDbContext` 或在 Controller 内调用 `SaveChangesAsync`、写 EF 查询。

| Controller | 原越层点 | 下沉方式 |
|---|---|---|
| KbController | `_uow.SaveChangesAsync()` | 实现 `KbService` 真实逻辑（`Upload/TogglePin/Delete/IncrementDownloadCount`），Controller 改调 `IKbService` |
| ProjectImportController | `_db.Projects.AddRangeAsync` + `_db.SaveChangesAsync()` | 新增 `IProjectService.ImportProjectsAsync(List<Project>)`（内部 `_uow.Projects.AddRangeAsync` + `_uow.SaveChangesAsync`）；`existingNos` 改走 `_uow.Projects.Query()` |
| AccountController | 登录时直查 `SysUserRoles`/`SysRoles` 拼角色 | 新增 `IUserService.GetRoleCodesAsync(long userId)`（仓储 Join） |
| ProjectController | 注入 `AppDbContext` 仅读 `SysConfigs` | 改为 `_uow.SysConfigs.Query()`，移除 `AppDbContext` 注入 |
| ReportController | 注入 `AppDbContext` 写回款/产值报表 EF 查询与导出 | 新建 `IReportService`/`ReportService`，查询、聚合、Excel 导出全部下沉；Controller 仅填 `ViewBag` + 路由分发 |
| DebugController | 多个 `_db.SaveChangesAsync()`（菜单/字典种子、迁移、缓存刷新） | 新建 `ISystemSeedService`/`SystemSeedService`（服务内部合法注入 `AppDbContext`，种子/迁移本质即 DbContext 级操作） |

配套：视图契约用**公开具体 DTO**（`ReceiptReportDto`/`OutputReportDto`/`ReceiptByDeptDto` 等）保住 `Views/Report/{Receipt,Output}.cshtml` 的 `dynamic` 绑定；`Program.cs` 注册 `IReportService`、`ISystemSeedService`。

## 二、其余审计项（此前子步骤，本轮已核验到位）

- **4.2 时区统一 UTC**：`grep DateTime.(Now|Today)` 在 `.cs` 中已 **0 匹配**（全部 `DateTime.UtcNow` / `DateTime.UtcNow.Date`）。
- **4.3 收尾**：移除手设 `CreatedAt`/`UpdatedAt`，由 `AppDbContext.SaveChangesAsync` 统一自动填充。
- **3.5 `GetPagedAsync` 返回形状统一**：`PagedResultBase` 增加 `WarnCount`；`Certificate/Contract` 服务填充，对应 Controller 读取。
- **3.3 内联分页统一**：`Project/User/Role/Employee/OperLog/Contract/Certificate` 服务及 `KbController` 统一走 `ToPagedAsync`。
- **2.4（上轮）**：`ConfigService`/`OperLogService` 由直连 `AppDbContext` 改为走 `IUnitOfWork`。
- **3.1 / 3.2 / 3.4 / 4.1 / 4.4 / 2.2 / 5（上轮）**：泛型 `BuildTree`、进度文本统一、文件删除统一、`.editorconfig`、字典类型常量、删除事务死代码、删除空 `SeedAsync`。

## 三、故意未做（按用户明确指示）

- **2.1** `IUnitOfWork` 上帝对象整体拆分（影响约 20 个文件，需单独规划）。
- **3.6** `CrudControllerBase`（用户明确指示跳过）。

## 四、踩坑记录（重要）

- 新建 `Services/DTOs/Report/ReportDtos.cs` 时，该路径已是**被跟踪的已存在文件**（原本定义了 `TemplateInfoDto` 等模板报告 DTO）。误用 `Write` 覆盖后，新增其他 `.cs` 触发全量重编，`ReportGeneratorService` 报 CS0246（找不到 `TemplateInfoDto`）。
  - 修复：用 `git checkout HEAD -- Services/DTOs/Report/ReportDtos.cs` 恢复原文件，再把报表 DTO 合并追加进去。
  - 教训：**新建文件前先 `Glob`/`Read` 探活，避免 `Write` 覆写已存在的已跟踪文件**。

## 五、最终验证

```
dotnet build -c Debug
→ 已成功生成。0 个错误
```
